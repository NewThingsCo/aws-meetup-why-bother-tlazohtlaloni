spring:
  application:
    name: aztec

server:
  port: 8890

eureka:
  client:
    serviceUrl:
      defaultZone: ${EUREKA_URI:http://localhost:8888/eureka}
      instance:
        preferIpAddress: true